 // await client.keys('*')


// 1 küçükten büyüğe sıralar
// -1 büyükten küçüğe sıralar
 

 https://protected-refuge-68834.herokuapp.com/


 sudo lsof -i :5000
 kill -9 <POD>


 \/api\/users\/((?!daily-earning)|(?!sezer).)*$




 git clone <my git project>
heroku create <app name>

#remove package.json
mv package.json tmp_package.json
git add package.json
git commit -m "removed package.json"

#re-add package.json
mv tmp_package.json package.json
git add package.json
git commit -m "re-added package.json"

git push heroku master


echo "# panteon-game" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Sayzera/panteon-game.git
git push -u origin main
